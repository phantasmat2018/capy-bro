<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!--
    Input control styles per project_design_guide.md §7.4-7.5 and §5.2.
    Four x:Key'd styles to be applied explicitly (same approach as
    Buttons.xaml — implicit styling would bleed into the editable
    ComboBox's nested TextBox and produce double-rounded corners).

      InputTextBox       §7.4   single/multi-line text entry
      InputPasswordBox   §7.4   masked entry — eye-toggle deferred (see below)
      InputComboBox      §7.5   selectable + editable combo
      InputComboBoxItem  §7.5   per-item hover/selected states inside Popup

    Each TextBox/PasswordBox uses its own ControlTemplate with a single
    Border + ScrollViewer x:Name="PART_ContentHost" (the well-known WPF
    template part — text rendering hooks into it). Border CornerRadius
    is the §5.2 6 px on inputs.

    State contract per §7.4:
      Rest      Layer1 / BorderDefault
      Hover     Layer1 / BorderStrong
      Focused   Layer1 / BorderFocus 2 px (no scale animation — inputs
                are precision targets, not click affordances)
      Disabled  Layer2 / BorderSubtle
      Invalid   (deferred — Validation.HasError trigger lands in
                 Phase D task 16 with the accessibility pass)

    Eye-toggle (§7.4 last paragraph) is tracked separately: PasswordBox
    can't simply swap to plaintext via PasswordChar — it needs a
    sibling TextBox + visibility swap. That'll land as
    Controls/RevealablePasswordBox.xaml in a follow-up commit, replacing
    the current PasswordBoxBindable usage in GeneralTab.
  -->

  <!-- ==================== InputTextBox (§7.4) ==================== -->
  <Style x:Key="InputTextBox" TargetType="TextBox">
    <Setter Property="Background" Value="{DynamicResource SurfaceLayer1Brush}" />
    <Setter Property="Foreground" Value="{DynamicResource OnSurfaceStrongBrush}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BorderDefaultBrush}" />
    <!--
      Constant 2 px BorderThickness across all states so the focus-ring
      trigger doesn't resize the control.  Pre-fix BorderThickness went
      from 1 (rest) to 2 (focus), which made the control grow by 2 px
      width + 2 px height when clicked — neighbouring elements visibly
      shifted (the eye-toggle next to the API-key field jumped right,
      sub-pixel rounding artefacts appeared on the rest of the row).
      Constant 2 px keeps the outer bounds fixed; the focus signal
      lives entirely in the BorderBrush colour change.
    -->
    <Setter Property="BorderThickness" Value="2" />
    <Setter Property="Padding" Value="10,6" />
    <Setter Property="MinHeight" Value="32" />
    <Setter Property="FontFamily" Value="{DynamicResource FontPrimary}" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="CaretBrush" Value="{DynamicResource OnSurfaceStrongBrush}" />
    <Setter Property="SelectionBrush" Value="{DynamicResource BrandPrimaryBrush}" />
    <Setter Property="SelectionOpacity" Value="0.4" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <!--
      Themed right-click menu — replaces the WPF/Win32 default which
      reads light-grey on light-grey against our dark canvas and
      shows OS-locale labels instead of our Translator strings.
      InputCutCopyPasteMenu lives in Themes/Menus.xaml; x:Shared=False
      on the resource means each TextBox holds its own instance.
    -->
    <Setter Property="ContextMenu" Value="{DynamicResource InputCutCopyPasteMenu}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TextBox">
          <Border x:Name="Root"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="6"
                  SnapsToDevicePixels="True">
            <ScrollViewer x:Name="PART_ContentHost"
                          Padding="{TemplateBinding Padding}"
                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                          Focusable="False"
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollBarVisibility="Hidden" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource BorderStrongBrush}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <!-- Colour-only focus change; BorderThickness stays at the
                   style-level 2 px so the layout slot is unchanged. -->
              <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource BorderFocusBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Root" Property="Background" Value="{DynamicResource SurfaceLayer2Brush}" />
              <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource BorderSubtleBrush}" />
              <Setter Property="Foreground" Value="{DynamicResource OnSurfaceDisabledBrush}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ==================== InputPasswordBox (§7.4) ==================== -->
  <Style x:Key="InputPasswordBox" TargetType="PasswordBox">
    <Setter Property="Background" Value="{DynamicResource SurfaceLayer1Brush}" />
    <Setter Property="Foreground" Value="{DynamicResource OnSurfaceStrongBrush}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BorderDefaultBrush}" />
    <!-- Constant 2 px BorderThickness — see InputTextBox style above
         for the rationale (no layout shift on focus). -->
    <Setter Property="BorderThickness" Value="2" />
    <Setter Property="Padding" Value="10,6" />
    <Setter Property="MinHeight" Value="32" />
    <Setter Property="FontFamily" Value="{DynamicResource FontPrimary}" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="CaretBrush" Value="{DynamicResource OnSurfaceStrongBrush}" />
    <Setter Property="SelectionBrush" Value="{DynamicResource BrandPrimaryBrush}" />
    <Setter Property="SelectionOpacity" Value="0.4" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="PasswordBox">
          <Border x:Name="Root"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="6"
                  SnapsToDevicePixels="True">
            <ScrollViewer x:Name="PART_ContentHost"
                          Padding="{TemplateBinding Padding}"
                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                          Focusable="False"
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollBarVisibility="Hidden" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource BorderStrongBrush}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <!-- Colour-only focus change; BorderThickness stays at the
                   style-level 2 px so the layout slot is unchanged. -->
              <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource BorderFocusBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Root" Property="Background" Value="{DynamicResource SurfaceLayer2Brush}" />
              <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource BorderSubtleBrush}" />
              <Setter Property="Foreground" Value="{DynamicResource OnSurfaceDisabledBrush}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ==================== InputComboBoxItem (§7.5) ==================== -->
  <!-- Defined first because InputComboBox's popup ItemContainerStyle
       references it via {StaticResource} resolution within the same
       dictionary. -->
  <Style x:Key="InputComboBoxItem" TargetType="ComboBoxItem">
    <Setter Property="Foreground" Value="{DynamicResource OnSurfaceStrongBrush}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Padding" Value="12,8" />
    <Setter Property="MinHeight" Value="32" />
    <Setter Property="FontFamily" Value="{DynamicResource FontPrimary}" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBoxItem">
          <Border x:Name="Root"
                  Background="{TemplateBinding Background}"
                  Padding="{TemplateBinding Padding}"
                  CornerRadius="4"
                  SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Root" Property="Background" Value="{DynamicResource SurfaceLayer2Brush}" />
            </Trigger>
            <Trigger Property="IsHighlighted" Value="True">
              <Setter TargetName="Root" Property="Background" Value="{DynamicResource SurfaceLayer3Brush}" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="Root" Property="Background" Value="{DynamicResource SurfaceLayer3Brush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource OnSurfaceDisabledBrush}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ==================== InputComboBox (§7.5) ==================== -->
  <!-- Custom template with two visual paths inside one Grid: the
       chevron + ContentPresenter for selectable mode, and the inline
       editable TextBox host for IsEditable=True. Switching between
       them is handled by the standard PART_EditableTextBox visibility
       toggle that ComboBox's IsEditable trigger drives. -->
  <Style x:Key="InputComboBox" TargetType="ComboBox">
    <Setter Property="Background" Value="{DynamicResource SurfaceLayer1Brush}" />
    <Setter Property="Foreground" Value="{DynamicResource OnSurfaceStrongBrush}" />
    <Setter Property="BorderBrush" Value="{DynamicResource BorderDefaultBrush}" />
    <!-- Constant 2 px BorderThickness — see InputTextBox style above
         for the rationale (no layout shift on focus). -->
    <Setter Property="BorderThickness" Value="2" />
    <Setter Property="Padding" Value="10,6" />
    <Setter Property="MinHeight" Value="32" />
    <Setter Property="FontFamily" Value="{DynamicResource FontPrimary}" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="ItemContainerStyle" Value="{StaticResource InputComboBoxItem}" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBox">
          <Grid>
            <ToggleButton x:Name="ToggleButton"
                          Background="{TemplateBinding Background}"
                          BorderBrush="{TemplateBinding BorderBrush}"
                          BorderThickness="{TemplateBinding BorderThickness}"
                          ClickMode="Press"
                          Focusable="False"
                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
              <ToggleButton.Template>
                <ControlTemplate TargetType="ToggleButton">
                  <Border x:Name="Root"
                          Background="{TemplateBinding Background}"
                          BorderBrush="{TemplateBinding BorderBrush}"
                          BorderThickness="{TemplateBinding BorderThickness}"
                          CornerRadius="6"
                          SnapsToDevicePixels="True">
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                      </Grid.ColumnDefinitions>
                      <ContentPresenter x:Name="ContentArea"
                                        Grid.Column="0"
                                        Margin="10,6"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center" />
                      <Path Grid.Column="1"
                            Width="14"
                            Height="14"
                            Margin="0,0,8,0"
                            Data="{DynamicResource Icon.ChevronDown}"
                            Stroke="{DynamicResource OnSurfaceMutedBrush}"
                            StrokeThickness="1.5"
                            StrokeStartLineCap="Round"
                            StrokeEndLineCap="Round"
                            StrokeLineJoin="Round"
                            Stretch="Uniform"
                            VerticalAlignment="Center" />
                    </Grid>
                  </Border>
                </ControlTemplate>
              </ToggleButton.Template>
            </ToggleButton>

            <!-- Inline editable host. Visibility flipped by the
                 IsEditable trigger below; default is Collapsed so the
                 ContentPresenter shows the SelectedItem instead.
                 ContextMenu explicit because a TextBox declared inside
                 a ControlTemplate doesn't pick up the InputTextBox
                 Setter — needs the attribute per-instance. -->
            <TextBox x:Name="PART_EditableTextBox"
                     Margin="10,6,28,6"
                     Background="Transparent"
                     BorderThickness="0"
                     Foreground="{TemplateBinding Foreground}"
                     CaretBrush="{DynamicResource OnSurfaceStrongBrush}"
                     SelectionBrush="{DynamicResource BrandPrimaryBrush}"
                     ContextMenu="{DynamicResource InputCutCopyPasteMenu}"
                     IsReadOnly="{TemplateBinding IsReadOnly}"
                     VerticalAlignment="Center"
                     Visibility="Collapsed">
              <TextBox.Template>
                <ControlTemplate TargetType="TextBox">
                  <ScrollViewer x:Name="PART_ContentHost"
                                Background="Transparent"
                                Focusable="False"
                                HorizontalScrollBarVisibility="Hidden"
                                VerticalScrollBarVisibility="Hidden" />
                </ControlTemplate>
              </TextBox.Template>
            </TextBox>

            <ContentPresenter x:Name="ContentSite"
                              Margin="10,6,28,6"
                              IsHitTestVisible="False"
                              Content="{TemplateBinding SelectionBoxItem}"
                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                              VerticalAlignment="Center" />

            <Popup x:Name="PART_Popup"
                   AllowsTransparency="True"
                   Placement="Bottom"
                   IsOpen="{TemplateBinding IsDropDownOpen}"
                   Focusable="False"
                   PopupAnimation="Slide">
              <Border CornerRadius="6"
                      Background="{DynamicResource SurfaceLayer1Brush}"
                      BorderBrush="{DynamicResource BorderDefaultBrush}"
                      BorderThickness="1"
                      MinWidth="{TemplateBinding ActualWidth}"
                      MaxHeight="320"
                      Margin="0,4,0,0"
                      SnapsToDevicePixels="True">
                <Border.Effect>
                  <DropShadowEffect ShadowDepth="2" BlurRadius="12" Opacity="0.18" />
                </Border.Effect>
                <ScrollViewer SnapsToDevicePixels="True">
                  <StackPanel IsItemsHost="True"
                              KeyboardNavigation.DirectionalNavigation="Contained" />
                </ScrollViewer>
              </Border>
            </Popup>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsEditable" Value="True">
              <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
              <Setter TargetName="ContentSite" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource OnSurfaceDisabledBrush}" />
              <Setter Property="Background" Value="{DynamicResource SurfaceLayer2Brush}" />
              <Setter Property="BorderBrush" Value="{DynamicResource BorderSubtleBrush}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <!-- Colour-only focus change; BorderThickness stays at the
                   style-level 2 px so the layout slot is unchanged. -->
              <Setter Property="BorderBrush" Value="{DynamicResource BorderFocusBrush}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
